{
  "devDependencies": {
    "hasura-cli": "^2.8.4"
  },
  "scripts": {
    "hasura-migrate": "hasura migrate apply --all-databases",
    "hasura-metadata": "hasura metadata apply",
    "hasura-console": "hasura console"
  }
}
