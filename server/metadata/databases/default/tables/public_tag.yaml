table:
  schema: public
  name: tag
object_relationships:
  - name: post_tag
    using:
      foreign_key_constraint_on:
        column: tag_id
        table:
          schema: public
          name: post_tag
array_relationships:
  - name: post_tags
    using:
      foreign_key_constraint_on:
        column: tag_id
        table:
          schema: public
          name: post_tag
insert_permissions:
  - role: user
    permission:
      check: {}
      columns:
        - name
      backend_only: false
select_permissions:
  - role: user
    permission:
      columns:
        - name
      filter: {}
update_permissions:
  - role: user
    permission:
      columns:
        - name
      filter: {}
      check: {}
